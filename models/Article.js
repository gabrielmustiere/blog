export default class Article {
  constructor (uid, titre, description, contenu, tags) {
    this.uid = uid;
    this.titre = titre;
    this.description = description;
    this.contenu = contenu;
    this.tags = tags;
  }
}
